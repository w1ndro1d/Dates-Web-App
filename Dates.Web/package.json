{
  "name": "dates",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:api": "node --env-file-if-exists=.env.local server/dev.js",
    "db:migrate": "node --env-file-if-exists=.env.local scripts/migrate.js",
    "test": "node --test tests/*.test.js",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "express": "5.2.1",
    "nodemailer": "10.0.10",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  }
}
